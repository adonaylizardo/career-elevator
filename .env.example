# PayPal payment link (shown on /checkout when set)
VITE_PAYPAL_LINK=

# Crypto payment (shown on /checkout when set)
VITE_CRYPTO_ADDRESS=
VITE_CRYPTO_NETWORK=

# Intake form submission endpoint (falls back to JSON download when unset)
VITE_FORM_ENDPOINT=
